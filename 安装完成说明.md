# 🎉 AI 新闻收集工具 - 安装完成！

## ✅ 已完成的设置

1. ✅ **PostgreSQL 数据库** - 已安装并启动
2. ✅ **Python 虚拟环境** - 已创建
3. ✅ **所有依赖** - 已安装（包括 Playwright）
4. ✅ **数据库表** - 已创建
5. ✅ **前端依赖** - 已安装

## ⚠️ 下一步：配置 API 密钥

### 1. 编辑配置文件

```bash
cd /Users/pingxn7/Desktop/x/backend
nano .env
```

### 2. 必须修改的配置（只需要这两个！）

找到这两行并替换为您的真实 API 密钥：

```bash
# 第 14 行：Twitter API 密钥
TWITTER_API_KEY=temp_key_for_setup
# 改为：
TWITTER_API_KEY=你的真实twitterapi密钥

# 第 18 行：Claude API 密钥  
ANTHROPIC_API_KEY=temp_key_for_setup
# 改为：
ANTHROPIC_API_KEY=你的真实claude密钥
```

### 3. 可选配置（如果不用可以禁用）

如果您暂时不想使用截图和邮件功能，找到这几行：

```bash
# 第 48-49 行：禁用可选功能
ENABLE_SCREENSHOT=False  # 不生成截图
ENABLE_EMAIL=False       # 不发送邮件
```

### 4. 配置接收邮箱（可选）

如果要使用邮件功能，修改第 36 行：

```bash
EMAIL_TO=your-email@example.com
# 改为：
EMAIL_TO=你的邮箱@example.com
```

## 📝 添加监听的 Twitter 账号

### 方法 1：编辑种子脚本（推荐）

```bash
cd /Users/pingxn7/Desktop/x/backend
nano scripts/seed_accounts.py
```

在 `ACCOUNTS_TO_MONITOR` 列表中添加您想监听的账号。

### 方法 2：使用 Python 直接添加

```bash
cd /Users/pingxn7/Desktop/x/backend
source venv/bin/activate
python
```

然后输入：

```python
from app.database import SessionLocal
from app.models.monitored_account import MonitoredAccount

db = SessionLocal()

# 添加账号
account = MonitoredAccount(
    user_id="你的Twitter用户ID",
    username="用户名",
    display_name="显示名称",
    is_active=True
)

db.add(account)
db.commit()
print(f"已添加: @{account.username}")

db.close()
exit()
```

## 🚀 启动系统

### 配置完成后，运行：

```bash
cd /Users/pingxn7/Desktop/x

# 方法 1：使用 Makefile（推荐）
make run

# 方法 2：手动启动
# 终端 1 - 后端
cd backend
export PATH="/usr/local/opt/postgresql@14/bin:$PATH"
source venv/bin/activate
uvicorn app.main:app --reload

# 终端 2 - 前端
cd frontend
npm run dev
```

## 🌐 访问系统

启动后访问：

- **前端界面**: http://localhost:3000
- **后端 API**: http://localhost:8000
- **API 文档**: http://localhost:8000/docs
- **健康检查**: http://localhost:8000/api/health

## 🔧 手动测试

### 1. 手动采集推文

```bash
cd /Users/pingxn7/Desktop/x/backend
export PATH="/usr/local/opt/postgresql@14/bin:$PATH"
source venv/bin/activate
python scripts/manual_collect.py
```

### 2. 手动生成摘要

```bash
cd /Users/pingxn7/Desktop/x/backend
export PATH="/usr/local/opt/postgresql@14/bin:$PATH"
source venv/bin/activate
python scripts/manual_summary.py
```

### 3. 查看系统状态

```bash
cd /Users/pingxn7/Desktop/x/backend
export PATH="/usr/local/opt/postgresql@14/bin:$PATH"
source venv/bin/activate
python scripts/check_status.py
```

## 📚 获取 API 密钥

### Twitter API (twitterapi.io)

1. 访问：https://twitterapi.io
2. 注册账号
3. 在控制台获取 API Key
4. 复制到 `backend/.env` 文件

### Claude API (Anthropic)

1. 访问：https://console.anthropic.com
2. 注册账号
3. 创建 API Key
4. 复制到 `backend/.env` 文件

### 获取 Twitter 用户 ID

访问：https://tweeterid.com/
输入 Twitter 用户名（例如：@elonmusk）
获取用户 ID（例如：44196397）

## ❓ 常见问题

### Q: 我必须要所有的 API 密钥吗？

**A:** 不是！只需要两个：
- ✅ Twitter API Key（twitterapi.io）
- ✅ Anthropic API Key（Claude）

AWS S3 和 Resend 是可选的，可以禁用。

### Q: 如何禁用截图和邮件功能？

**A:** 编辑 `backend/.env`：

```bash
ENABLE_SCREENSHOT=False
ENABLE_EMAIL=False
```

### Q: 系统会自动运行吗？

**A:** 是的！启动后会自动：
- 每 2 小时采集一次推文
- 每天早上 8 点生成摘要
- 自动发送邮件（如果配置了）

### Q: 如何停止系统？

**A:** 按 `Ctrl+C` 停止运行的程序

## 📖 完整文档

查看更多详细信息：

- **中文使用指南.md** - 完整的中文使用说明
- **README.md** - 英文主文档
- **QUICKSTART.md** - 快速开始指南
- **FAQ.md** - 常见问题解答

## 🎯 推荐监听的 AI 账号

**中文账号：**
- @dotey（宝玉）
- @op7418（歸藏）

**英文账号：**
- @sama（Sam Altman - OpenAI CEO）
- @karpathy（Andrej Karpathy）
- @OpenAI
- @AnthropicAI
- @ylecun（Yann LeCun）

## 💡 快速开始流程

```bash
# 1. 配置 API 密钥
cd /Users/pingxn7/Desktop/x/backend
nano .env
# 修改 TWITTER_API_KEY 和 ANTHROPIC_API_KEY

# 2. 添加监听账号（可选，已有默认账号）
nano scripts/seed_accounts.py
source venv/bin/activate
python scripts/seed_accounts.py

# 3. 启动系统
cd /Users/pingxn7/Desktop/x
make run

# 4. 访问界面
# 打开浏览器: http://localhost:3000
```

## 🎉 完成！

现在您只需要：
1. 配置 API 密钥
2. 添加想监听的账号
3. 启动系统

就可以开始使用了！

---

**需要帮助？** 查看 `中文使用指南.md` 或 `FAQ.md`
