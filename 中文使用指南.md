# AI 新闻收集工具 - 中文使用指南

## 📋 目录

1. [快速开始](#快速开始)
2. [配置 API 密钥](#配置-api-密钥)
3. [添加监听的 Twitter 账号](#添加监听的-twitter-账号)
4. [配置接收邮箱](#配置接收邮箱)
5. [运行系统](#运行系统)
6. [常见问题](#常见问题)

---

## 🚀 快速开始

### 前置要求

- Python 3.9 或更高版本
- Node.js 18 或更高版本
- PostgreSQL 14 或更高版本

### 一键安装

```bash
cd /Users/pingxn7/Desktop/x
./setup.sh
```

这个脚本会自动：
- 创建 Python 虚拟环境
- 安装所有依赖
- 创建数据库
- 运行数据库迁移
- 创建配置文件模板

---

## 🔑 配置 API 密钥

### 1. 编辑后端配置文件

```bash
cd backend
nano .env
```

或使用任何文本编辑器打开 `backend/.env` 文件。

### 2. 必需的 API 密钥

#### ✅ Twitter API 密钥（必需）

**获取方式：**
1. 访问 https://twitterapi.io
2. 注册账号
3. 在控制台获取 API Key

**配置：**
```bash
TWITTER_API_KEY=你的_twitterapi_密钥
```

**注意：**
- 这是 twitterapi.io 的 API Key，不是 Twitter 官方的 API
- twitterapi.io 提供更简单的 Twitter 数据访问
- 不需要 Twitter 官方的 user_id，只需要这一个 API Key

#### ✅ Anthropic Claude API 密钥（必需）

**获取方式：**
1. 访问 https://console.anthropic.com
2. 注册账号
3. 创建 API Key

**配置：**
```bash
ANTHROPIC_API_KEY=你的_anthropic_密钥
```

#### ⚠️ AWS S3 配置（可选，用于截图存储）

如果不配置，截图功能会被禁用，但系统仍可正常运行。

**获取方式：**
1. 访问 AWS 控制台
2. 创建 S3 存储桶
3. 创建 IAM 用户并获取访问密钥

**配置：**
```bash
AWS_ACCESS_KEY_ID=你的_aws_访问密钥
AWS_SECRET_ACCESS_KEY=你的_aws_密钥
AWS_S3_BUCKET=你的存储桶名称
AWS_REGION=us-east-1
```

**如果不使用 S3：**
```bash
ENABLE_SCREENSHOT=False
```

#### ⚠️ Resend 邮件服务（可选，用于发送邮件）

如果不配置，邮件功能会被禁用，但系统仍可正常运行。

**获取方式：**
1. 访问 https://resend.com
2. 注册账号
3. 获取 API Key

**配置：**
```bash
RESEND_API_KEY=你的_resend_密钥
EMAIL_FROM=noreply@你的域名.com
```

**如果不使用邮件：**
```bash
ENABLE_EMAIL=False
```

### 3. 完整配置示例

```bash
# ========== 必需配置 ==========

# Twitter API（必需）
TWITTER_API_KEY=your_twitterapi_io_key_here

# Claude API（必需）
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxx

# 数据库（必需）
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ai_news

# ========== 可选配置 ==========

# AWS S3（可选，用于截图）
AWS_ACCESS_KEY_ID=your_aws_key
AWS_SECRET_ACCESS_KEY=your_aws_secret
AWS_S3_BUCKET=ai-news-screenshots
AWS_REGION=us-east-1

# 邮件服务（可选）
RESEND_API_KEY=re_xxxxxxxxxxxxx
EMAIL_FROM=noreply@yourdomain.com
EMAIL_TO=your-email@example.com

# 功能开关
ENABLE_SCREENSHOT=True    # 如果没有 S3，设为 False
ENABLE_TRANSLATION=True   # 是否翻译为中文
ENABLE_EMAIL=True         # 如果没有 Resend，设为 False

# ========== 高级配置 ==========

# 采集频率（使用 cron 表达式）
SCHEDULE_TWEET_COLLECTION_CRON=0 */2 * * *  # 每2小时采集一次
SCHEDULE_DAILY_SUMMARY_CRON=0 8 * * *       # 每天早上8点生成摘要

# 排名权重
ENGAGEMENT_WEIGHT_LIKES=1.0        # 点赞权重
ENGAGEMENT_WEIGHT_RETWEETS=2.0     # 转发权重
ENGAGEMENT_WEIGHT_REPLIES=1.5      # 回复权重
ENGAGEMENT_WEIGHT_BOOKMARKS=2.5    # 收藏权重
AI_RELEVANCE_WEIGHT=3.0            # AI 相关性权重

# 精选数量
TOP_TWEETS_COUNT=10  # 每日精选推文数量
```

---

## 👥 添加监听的 Twitter 账号

### 方法 1：编辑种子脚本（推荐）

1. **打开账号配置文件：**

```bash
cd backend
nano scripts/seed_accounts.py
```

2. **添加您想监听的账号：**

找到 `ACCOUNTS_TO_MONITOR` 列表，添加新账号：

```python
ACCOUNTS_TO_MONITOR = [
    {
        "user_id": "44196397",           # Twitter 用户 ID
        "username": "elonmusk",          # Twitter 用户名（@后面的部分）
        "display_name": "Elon Musk",     # 显示名称
    },
    {
        "user_id": "33836629",
        "username": "ylecun",
        "display_name": "Yann LeCun",
    },
    # 添加您自己的账号
    {
        "user_id": "你的用户ID",
        "username": "账号用户名",
        "display_name": "显示名称",
    },
]
```

3. **如何获取 Twitter 用户 ID：**

**方法 A：使用在线工具**
- 访问 https://tweeterid.com/
- 输入 Twitter 用户名（例如：@elonmusk）
- 获取用户 ID

**方法 B：使用 Twitter API**
```bash
# 如果您已经有 twitterapi.io 的 key
curl -H "Authorization: Bearer YOUR_API_KEY" \
  "https://api.twitterapi.io/v1/users/by/username/elonmusk"
```

4. **运行种子脚本：**

```bash
cd backend
source venv/bin/activate
python scripts/seed_accounts.py
```

### 方法 2：直接在数据库中添加

```bash
cd backend
source venv/bin/activate
python
```

然后在 Python 交互式环境中：

```python
from app.database import SessionLocal
from app.models.monitored_account import MonitoredAccount

db = SessionLocal()

# 添加新账号
new_account = MonitoredAccount(
    user_id="123456789",           # Twitter 用户 ID
    username="example_user",       # 用户名
    display_name="示例用户",        # 显示名称
    is_active=True                 # 是否启用
)

db.add(new_account)
db.commit()
print(f"已添加账号: @{new_account.username}")

db.close()
```

### 推荐监听的 AI 相关账号

```python
# 中文 AI 账号
{
    "user_id": "待查询",
    "username": "dotey",
    "display_name": "宝玉",
},
{
    "user_id": "待查询",
    "username": "op7418",
    "display_name": "歸藏",
},

# 英文 AI 账号
{
    "user_id": "1467726470533754880",
    "username": "sama",
    "display_name": "Sam Altman",
},
{
    "user_id": "17919972",
    "username": "karpathy",
    "display_name": "Andrej Karpathy",
},
{
    "user_id": "2861417424",
    "username": "OpenAI",
    "display_name": "OpenAI",
},
{
    "user_id": "1603818258",
    "username": "AnthropicAI",
    "display_name": "Anthropic",
},
```

---

## 📧 配置接收邮箱

### 1. 在配置文件中设置

编辑 `backend/.env` 文件：

```bash
# 发件人邮箱（需要在 Resend 中验证）
EMAIL_FROM=noreply@yourdomain.com

# 收件人邮箱（您的邮箱）
EMAIL_TO=your-email@example.com
```

### 2. 配置多个收件人

如果想发送给多个邮箱，修改 `backend/app/services/email_service.py`：

```python
# 找到 send_daily_digest 方法
async def send_daily_digest(
    self,
    summary: DailySummary,
    highlights: list[ProcessedTweet],
    recipient: Optional[str] = None
) -> bool:
    # 修改这里，添加多个收件人
    recipients = [
        "email1@example.com",
        "email2@example.com",
        "email3@example.com"
    ]

    params = {
        "from": settings.email_from,
        "to": recipients,  # 改为列表
        "subject": f"AI News Digest - {date_str} | 今日精选{len(highlights)}条",
        "html": html_body
    }
```

### 3. 不使用邮件功能

如果您不想使用邮件功能，可以禁用它：

```bash
# 在 backend/.env 中设置
ENABLE_EMAIL=False
```

系统仍会生成每日摘要，您可以通过网页界面查看：
- 访问 http://localhost:3000

---

## 🎮 运行系统

### 方法 1：使用 Makefile（推荐）

```bash
# 同时启动后端和前端
make run
```

### 方法 2：分别启动

**终端 1 - 启动后端：**
```bash
cd backend
source venv/bin/activate
uvicorn app.main:app --reload
```

**终端 2 - 启动前端：**
```bash
cd frontend
npm run dev
```

### 访问系统

- **前端界面**: http://localhost:3000
- **后端 API**: http://localhost:8000
- **API 文档**: http://localhost:8000/docs
- **健康检查**: http://localhost:8000/api/health

---

## 🔧 手动操作

### 手动采集推文

```bash
cd backend
source venv/bin/activate
python scripts/manual_collect.py
```

### 手动生成摘要

```bash
cd backend
source venv/bin/activate
python scripts/manual_summary.py
```

### 查看系统状态

```bash
cd backend
source venv/bin/activate
python scripts/check_status.py
```

输出示例：
```
📊 MONITORED ACCOUNTS
   Total: 15
   Active: 15

🐦 TWEETS
   Total collected: 1250
   Processed: 890
   Unprocessed: 360

🤖 AI ANALYSIS
   Total analyzed: 890
   AI-related: 634 (71.2%)
   Not AI-related: 256

📧 DAILY SUMMARIES
   Total: 45
   Emails sent: 42
   Latest: 2024-02-06 (87 tweets, 10 highlights)
```

---

## ❓ 常见问题

### Q1: 我必须要有所有的 API 密钥吗？

**A:** 不是的！只需要两个必需的密钥：

**必需：**
- ✅ Twitter API Key（twitterapi.io）
- ✅ Anthropic API Key（Claude）

**可选：**
- ⚠️ AWS S3（用于截图，可以禁用）
- ⚠️ Resend（用于邮件，可以禁用）

如果不配置可选服务，在 `.env` 中设置：
```bash
ENABLE_SCREENSHOT=False
ENABLE_EMAIL=False
```

### Q2: twitterapi.io 的 API Key 在哪里获取？

**A:**
1. 访问 https://twitterapi.io
2. 注册账号（可能需要付费，具体看他们的定价）
3. 在控制台找到 "API Keys" 或 "Credentials"
4. 复制 API Key

**注意：** 这不是 Twitter 官方 API，而是第三方服务，使用更简单。

### Q3: 我不知道 Twitter 用户的 user_id 怎么办？

**A:** 使用在线工具：
1. 访问 https://tweeterid.com/
2. 输入 Twitter 用户名（例如：elonmusk）
3. 获取 user_id（例如：44196397）

或者使用这个工具：https://www.codeofaninja.com/tools/find-twitter-id/

### Q4: 如何修改采集频率？

**A:** 编辑 `backend/.env` 文件：

```bash
# 每2小时采集一次（默认）
SCHEDULE_TWEET_COLLECTION_CRON=0 */2 * * *

# 改为每小时采集一次
SCHEDULE_TWEET_COLLECTION_CRON=0 * * * *

# 改为每4小时采集一次
SCHEDULE_TWEET_COLLECTION_CRON=0 */4 * * *

# 改为每天早上9点采集
SCHEDULE_TWEET_COLLECTION_CRON=0 9 * * *
```

Cron 表达式格式：`分钟 小时 日 月 星期`

### Q5: 如何修改每日摘要的发送时间？

**A:** 编辑 `backend/.env` 文件：

```bash
# 每天早上8点（默认）
SCHEDULE_DAILY_SUMMARY_CRON=0 8 * * *

# 改为每天早上9点
SCHEDULE_DAILY_SUMMARY_CRON=0 9 * * *

# 改为每天晚上8点
SCHEDULE_DAILY_SUMMARY_CRON=0 20 * * *
```

### Q6: 如何修改精选推文的数量？

**A:** 编辑 `backend/.env` 文件：

```bash
# 默认每天精选10条
TOP_TWEETS_COUNT=10

# 改为精选15条
TOP_TWEETS_COUNT=15

# 改为精选5条
TOP_TWEETS_COUNT=5
```

### Q7: 系统会自动运行吗？

**A:** 是的！启动后，系统会自动：
- 每2小时采集一次推文
- 每天早上8点生成摘要
- 自动发送邮件（如果配置了）

您只需要保持程序运行即可。

### Q8: 如何停止系统？

**A:**
- 如果使用 `make run`：按 `Ctrl+C`
- 如果分别启动：在两个终端都按 `Ctrl+C`
- 如果使用 Docker：`docker-compose down`

### Q9: 数据存储在哪里？

**A:**
- **推文数据**: PostgreSQL 数据库
- **截图**: AWS S3（如果配置了）
- **日志**: `backend/logs/` 目录

### Q10: 如何备份数据？

**A:** 备份数据库：

```bash
# 导出数据库
pg_dump ai_news > backup_$(date +%Y%m%d).sql

# 恢复数据库
psql ai_news < backup_20240206.sql
```

### Q11: 成本大概是多少？

**A:** 每月约 $20-60：
- Claude API: $8-25
- Twitter API: $0-20（取决于 twitterapi.io 的定价）
- AWS S3: $1-3（如果使用）
- Resend: $0-10（免费额度：3000封邮件/月）
- 服务器: 如果本地运行则免费

### Q12: 可以监听中文 Twitter 账号吗？

**A:** 当然可以！系统支持任何语言的 Twitter 账号。AI 会自动识别 AI 相关内容，并提供中文翻译。

---

## 🎯 完整配置流程示例

### 步骤 1: 安装系统

```bash
cd /Users/pingxn7/Desktop/x
./setup.sh
```

### 步骤 2: 配置 API 密钥

编辑 `backend/.env`：

```bash
# 必需
TWITTER_API_KEY=your_twitterapi_key_here
ANTHROPIC_API_KEY=sk-ant-your_key_here

# 可选（如果不用就设为 False）
ENABLE_SCREENSHOT=False
ENABLE_EMAIL=False
```

### 步骤 3: 添加监听账号

编辑 `backend/scripts/seed_accounts.py`，添加您想监听的账号。

运行：
```bash
cd backend
source venv/bin/activate
python scripts/seed_accounts.py
```

### 步骤 4: 配置邮箱（可选）

如果要使用邮件功能，在 `backend/.env` 中：

```bash
RESEND_API_KEY=your_resend_key
EMAIL_TO=your-email@example.com
ENABLE_EMAIL=True
```

### 步骤 5: 启动系统

```bash
make run
```

### 步骤 6: 访问界面

打开浏览器访问：http://localhost:3000

---

## 📞 获取帮助

如果遇到问题：

1. **查看日志**：
   ```bash
   # 后端日志
   tail -f backend/logs/*.log

   # 或查看终端输出
   ```

2. **检查系统状态**：
   ```bash
   make status
   ```

3. **查看 API 文档**：
   访问 http://localhost:8000/docs

4. **查看更多文档**：
   - `README.md` - 主文档
   - `FAQ.md` - 常见问题（英文）
   - `QUICKSTART.md` - 快速开始指南（英文）

---

## 🎉 开始使用

现在您已经了解了如何配置系统，可以开始使用了！

```bash
# 1. 配置 API 密钥
nano backend/.env

# 2. 添加监听账号
nano backend/scripts/seed_accounts.py
python backend/scripts/seed_accounts.py

# 3. 启动系统
make run

# 4. 访问界面
# 打开浏览器: http://localhost:3000
```

祝您使用愉快！🚀
